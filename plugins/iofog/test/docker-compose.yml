services:
  test-runner:
    container_name: test-runner
    image: iofog/test-runner-develop:latest
    network_mode: bridge
    volumes:
    - ../../../conf:/conf
version: '3'
volumes:
  conf: null
